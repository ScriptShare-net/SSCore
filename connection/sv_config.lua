local SSCore = exports["SSCore"]

--Whitelist Message
SSCore:SetConfigValue("WhitelistMessage", "Thanks for joining. Sorry to be a bother but our server is currently discord whitelist. Basically all you need to do is join the discord and you can play. We have made it super simple. All you need to do is click the button below and join.")

--Queue Banner
SSCore:SetConfigValue("ServerBanner", "https://scriptshare.net/banner.png")

--Link to appeal bans
SSCore:SetConfigValue("BanAppealLink", "https://scriptshare.net/forums/banappeals")

--Queue message
SSCore:SetConfigValue("QueueMessage", "Thank you for choosing our server.")

--Player slots (Minus reserved slots)
SSCore:SetConfigValue("Slots", 1)

--Enable Connection Commands
SSCore:SetConfigValue("ConnectionCommands", true)

--Whitelist
SSCore:SetConfigValue("ServerWhitelist", false)