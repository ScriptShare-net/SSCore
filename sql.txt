SSCore Database
    - Users(PK identifier, permid, groups)
    - Character(FK identifier, characterSlot, firstname, lastname, skin, age, job, group, position, status)
        foreign key (identifier) references Users.identifier
    - identifiers(identifier, discords, steams, gtas, tokens, lives, xboxs, ips, fivems, gta2s)
    - bans(FK identifier, identifiers, time, reason, bannedBy)
        foreign key (identifier) references Users.identifier
    - inventory(FK identifier, FK characterSlot, items, weapons, accounts)
        foreign key (identifier) references Users.identifier
        foreign key (characterSlot) references Users.characterSlot
    - businesses(PK businessesName, businessLabel, accounts (DEFUALT NIL), whitelisted, enabled)
    - bussinessRoles(FK businessesName, roleName, roleLabel, pay, outfit)
        foreign key (businessesName) references business.businessesName
